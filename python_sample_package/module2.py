""" This is Modules 2"""


def mean(numbers: list) -> float:
    """Calculate the Mean

    Args:
        numbers (list): list of Integers

    Returns:
        value_of_mean : float
    """
    return sum(numbers)/len(numbers)
